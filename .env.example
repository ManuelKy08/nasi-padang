# Rename to .env and adjust values for your machine.
# NEVER commit a real .env file.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=rantau
DB_PASSWORD=rantau_secret_2026
DB_NAME=rantau

# Use a long, random string in production.
SESSION_KEY=change-me-to-a-long-random-secret-key-2026

PORT=:8080

# Restaurant operating hours (uses the server clock).
OPEN_HOUR=10
CLOSE_HOUR=22

# Delivery fee in Rupiah.
DELIVERY_FEE=5000